Release notes on translator's schematics

In our setup, the "BT" connector links to a Bluetooth module.
The link is a UART with flow control using 0-3.3V voltage levels.
One could use a USB using an FTDI chip instead of the Bluetooth module.
