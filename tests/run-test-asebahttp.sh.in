#!/bin/bash

# run this from cmake
# bash works on Windows if we run under MSYS2 / MINGW

killall aseba{dummynode,switch,massloader}

ASEBADUMMYNODE 0 &
sleep 2
TESTASEBAHTTP
status=$?
[ -z "$(jobs -p)" ] || kill %%
exit $status
